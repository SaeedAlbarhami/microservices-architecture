spring:
  application:
    name: studentservice
  profiles.active: development

---
spring:
  profiles: development
  cloud:
    config:
      uri: http://172.26.150.161:8081
---
spring:
  profiles: production
  cloud:
    config:
      uri: http://localhost:8081
